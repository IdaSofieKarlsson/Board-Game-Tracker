# Server
PORT=3001
NODE_ENV=development

# MongoDB (Prisma)
DATABASE_URL="mongodb+srv://USER:PASSWORD@HOST/DATABASE?retryWrites=true&w=majority"

# Firebase Admin (choose ONE approach)
# Option A: Path to service account JSON (local dev)
FIREBASE_SERVICE_ACCOUNT_PATH=""

# Option B: Service account JSON as a single-line string (CI/Vercel later)
FIREBASE_SERVICE_ACCOUNT_JSON=""
